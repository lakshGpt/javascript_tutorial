// DOM - document object model

// after parsing whole html file the browser creates a document Object 
// and this document get add into window Object

console.log(window);
console.log(window.document);   // normal representation
console.dir(document);   // javascript representation